[pytest]
testpaths = tests
asyncio_mode = auto
addopts =
    --strict-markers
    -v
    --tb=short
markers =
    unit: Unit tests
    integration: Integration tests
    security: Security tests
    regression: Regression tests
