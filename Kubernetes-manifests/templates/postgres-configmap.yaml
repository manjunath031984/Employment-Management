apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "employment-management.postgresName" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "employment-management.labels" . | nindent 4 }}
data:
  POSTGRES_DB: {{ .Values.postgres.dbName | quote }}