apiVersion: v1
kind: Service
metadata:
  name: employment-management
  labels:
    app: employment-management
spec:
  type: ClusterIP
  selector:
    app: employment-management
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP